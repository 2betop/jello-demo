#if ($menus)

    ## velocity 宏示例
    #macro( renderMenus $menus $child)
        #if( $child )
        <ul class="dropdown-menu" role="menu">
        #else
        <ul class="nav navbar-nav" role="menu">
        #end

        #foreach( $menu in $menus )
            #if( $menu.children )
            <li class="dropdown #if( $menu.active )active#end">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">$!{menu.label} <span class="caret"></span></a>

                #if( $menu.children )
                #renderMenus($menu.children true)
                #end
            </li>
            #else
            <li #if( $menu.active )class="active"#end>
                <a href="$!{menu.href}">$!{menu.label}</a>
            </li>
            #end
        #end

        </ul>
    #end

    <!-- Fixed navbar -->
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <a class="navbar-brand" href="/">Jello Demo</a>
            #renderMenus($menus false)
        </div>
    </div>
#end
